apiVersion: apps/v1
kind: Deployment
metadata:
  name: churnops-inference
spec:
  template:
    spec:
      containers:
        - name: inference-api
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
